import { ISong } from "../../models/songs";
import { IPerformer } from "../../models/performer";
import { ISet } from "../../models/set";

export const setSongPerformer = (song: ISong, performer: IPerformer) => {

}

export const setSongSet = (song: ISong, set: ISet) => {
    
}